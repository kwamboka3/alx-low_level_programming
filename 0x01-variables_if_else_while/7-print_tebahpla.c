#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always o (success/correct)
 */
int main(void)
{
char alph;
for (alph = 122; alph =97; alph--)
{
putchar (alph);
}
putchar ('\n');
return (0);
}
