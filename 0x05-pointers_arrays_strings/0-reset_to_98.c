#include "main.h"
#include <stdio.h>
/**
 * rest_to_98: takes a pointer to an int as parameter and updates value
 * Return: Always 0
 */

void rest_to_98(int *n)
{
int n;
n = 0;
putchar(n);
reset_to_98(&n);
putchar(n);
return (0);
}
