/**
 * swap_int: swipes the two numbers
 * Return: always 0
 */
#include <stdio.h>
#include "main.h"
void swap_int(int *a, int *b)
{
int temp = *a;
*a = *b;
*b = temp;
putchar(int *a);
putchar(int *b);
putchar('\n');
}
