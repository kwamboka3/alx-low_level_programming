#include "main.h"
#include <stdio.h>
/**
 * print_number - prints an integer
 * @n: print integer
 * Return: Always 0.
 */
void print_number(int n)
{
if (n >= 48 && n <= 57)
{
_putchar(n);
}
else
{
return (0);
}
}
